A26 customer portal invite using A27 dedupe gate. Read first: A22 token-mint route via grep -n 'apply.*token|generateApplyToken|/api/apply' server/ to match its shape (token table, single-use, expiry, redaction). Also read client/src/pages/customers/show.tsx where the duplicate banner lives, and the A30b user redaction allowlist (the field exclusion list around /api/admin/users response). Server changes: (Item 1) New endpoint POST /api/customers/:id/portal-invite. Auth: Admin / Manager / Master Admin only — mirror A22's permissions decorator. (Item 2) Mint a single-use token bound to customer_id, expires 24h. Reuse A22's token table if shape allows (add a nullable customer_id column if needed — non-destructive). If A22 supports polymorphic binding via subject_type / subject_id, use that instead. Whatever requires the least new schema. (Item 3) Return { url, expires_at }. URL https://<host>/portal/customer/<token>. (Item 4) Token redemption GET /api/portal/customer/:token validates, marks used, starts portal session, redirects to existing customer-portal landing route. (Item 5) Token redemption response must NOT include storedPassword, recoveryToken, googleAccessToken, googleRefreshToken — mirror A30b literally. Client changes in client/src/pages/customers/show.tsx: (Item 6) Send Portal Invite button in page header next to Edit Customer / Archive. Show only when customer has at least 1 email; else disabled with tooltip 'Add an email first.' (Item 7) Click handler reads the duplicates query you already added in A27 (cached, no extra fetch). If duplicates.length > 0 open confirmation modal listing labels: 'This customer shares email/phone with N other record(s): <labels>. The invite link binds to THIS record only.' Cancel default / Send to this customer. If no duplicates OR confirmed POST /api/customers/:id/portal-invite. Copy URL via navigator.clipboard.writeText. Toast: 'Invite link copied to clipboard. Expires in 24h.' (Item 8) Do NOT trigger an email send. Constraints: tenant-scope every server query. At most ONE new nullable column on token table. ts-check files you change. Show diff. No auto-Republish.