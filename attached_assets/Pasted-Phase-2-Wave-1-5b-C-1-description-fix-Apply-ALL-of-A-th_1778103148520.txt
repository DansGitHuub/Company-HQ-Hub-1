Phase 2 Wave 1.5b — §C.1 description fix. Apply ALL of A through D as one batch, then run TS check and stop. Don't hit Republish — Dan will.

A. server/companyCamRoutes.ts

1) Add a small extraction helper near the existing utility helpers in this file (or near the top of the receiver):

function extractCompanyCamDescription(raw: any): string | null {
  if (raw == null) return null;
  if (typeof raw === 'string') return raw.trim() || null;
  if (typeof raw === 'object' && typeof raw.plain_text_content === 'string') {
    return raw.plain_text_content.trim() || null;
  }
  return null;
}

2) Find the photo.created case in the webhook event switch. Wherever it currently extracts description (likely something like payload.photo.description), replace with extractCompanyCamDescription(payload?.photo?.description ?? payload?.description). Keep every other field extraction unchanged.

3) Add a new case 'photo.updated': that runs ON CONFLICT (companycam_photo_id) DO UPDATE — same INSERT statement as photo.created but with the conflict clause:
   SET description = EXCLUDED.description,
       raw_payload = EXCLUDED.raw_payload,
       captured_at = COALESCE(EXCLUDED.captured_at, companycam_photos.captured_at)

   DO NOT add updated_at = NOW() to the SET clause. companycam_photos has no updated_at column (§4.8 — that exact bug is what broke v1.1.0 → v1.1.1 last session).

4) Keep the outer catch returning 200 (§4.4). The new sub-step may have its own try/catch but the outer webhook handler always responds 200, regardless of inner errors.

B. server/estimateRoutes.ts (or wherever admin routes are registered — grep STAFF_ROLES first to find the existing pattern):

Add this endpoint alongside the existing PATCH endpoints. Use the same auth middleware as the existing Wave 1.5a endpoints.

app.post("/api/admin/companycam/backfill-descriptions", requireAuth, requireRoles(STAFF_ROLES), async (req, res) => {
  try {
    const { rows } = await pool.query(
      `SELECT companycam_photo_id FROM companycam_photos WHERE description IS NULL ORDER BY captured_at DESC NULLS LAST`
    );
    const results: { id: string; status: 'updated'|'still_null'|'api_error'; description?: string|null }[] = [];
    for (const r of rows) {
      const id = r.companycam_photo_id;
      try {
        const resp = await fetch(`https://api.companycam.com/v2/photos/${id}`, {
          headers: { Authorization: `Bearer ${process.env.COMPANYCAM_API_TOKEN}` }
        });
        if (!resp.ok) { results.push({ id, status: 'api_error' }); continue; }
        const j: any = await resp.json();
        const desc =
          typeof j?.description === 'string'
            ? (j.description.trim() || null)
            : (typeof j?.description?.plain_text_content === 'string'
                ? (j.description.plain_text_content.trim() || null)
                : null);
        if (desc) {
          await pool.query(
            `UPDATE companycam_photos SET description = $1 WHERE companycam_photo_id = $2`,
            [desc, id]
          );
          results.push({ id, status: 'updated', description: desc });
        } else {
          results.push({ id, status: 'still_null' });
        }
      } catch (err) {
        console.error('[companycam.backfill] photo', id, err);
        results.push({ id, status: 'api_error' });
      }
    }
    res.json({ scanned: rows.length, results });
  } catch (err) {
    console.error('[companycam.backfill] error', err);
    res.status(500).json({ error: 'backfill failed' });
  }
});

C. server/index.ts — bump the boot marker to:
[boot] estimate.companycam ui v1.1.2 — Phase 2 Wave 1.5b (description fix) — outer catch returns 200 (§10.3)

D. Verify TS baseline. Run NODE_OPTIONS=--max-old-space-size=4096 npx tsc --noEmit. Expected: 233 errors ± a couple new in the modified files. Investigate any net increase before stopping.

Do NOT touch: shared/schema.ts (no schema changes), server/migrations/companyCamPhase1.ts (no migration), client/src/components/CompanyCamSection.tsx (no UI changes). Do NOT add new dependencies. Do NOT Republish — that's Dan's call.